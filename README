mod_index_json 0.0.1

This module adds directory index handler, it responced by text/json.

To install:
  # make && make install

To enable this module:
  For example, see index_json.conf for CentOS.

  $ curl localhost/             # Return ["index.html"] by this module.
  $ curl localhost/index.html   # Return index.html by Apache-internal.
